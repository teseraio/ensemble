apiVersion: v1
kind: Service
metadata:
  name: {{ include "ensemble.fullname" . }}
  labels:
    {{- include "ensemble.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ensemble.selectorLabels" . | nindent 4 }}
