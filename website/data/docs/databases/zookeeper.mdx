---
title: 'Zookeeper'
---

Zookeeper is a hierarchical key-value store.

```yaml
apiVersion: "ensembleoss.io/v1"
kind: Cluster
metadata:
    name: <name>
spec:
    backend:
        name: zookeeper
    sets:
    - replicas: <replicas>
```
