---
title: 'Cassandra'
---

Cassandra is a NoSQL wide column store.

```yaml
apiVersion: "ensembleoss.io/v1"
kind: Cluster
metadata:
    name: <name>
spec:
    backend:
        name: cassandra
    sets:
    - replicas: <replicas>
```
