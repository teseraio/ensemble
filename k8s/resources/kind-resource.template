{
    "apiVersion": "ensembleoss.io/v1",
    "kind": "Resource",
    "metadata": {
        "name": "{{.Name}}"
    },
    "spec": {
        "cluster": "{{.Cluster}}",
        "resource": "{{.Resource}}"
    }
}